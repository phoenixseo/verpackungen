# GIT-IGNORE for Branch: Master
# Auskommentiert: nicht-ignorieren (also einchecken)


# ***** ALLGEMEINES ZEUGS *************************************************
# Dateien mit '._Name' oder '._.Name' ignorieren.
# Werden von macOs angelegt und sind uninteressant.
._*
._.*

# Von einem Dienst auf dem NAS autom. erzeugte Verzeichnisse, ignorieren.
*@eaDir*

# Ignore files generated by PhpStorm
.idea/

# Ignore apple stuff
.DS_Store

# Ignore Acquia Dev Desktop configs.
/web/sites/*.dd


# ***** DRUPAL ZEUGS *******************************************************
# Die .env Datei enthält DB-Passworte und andere geheime Infos.
# Soll ignoriert werden (nicht einchecken).
# Die .env Datei muss am Zielort per FTP eingerichtet werden.
/.env


# Ordner assets: Backups, DB-Dumps, Favicons, SSL-Zertifikat u.a.
# assets
#
# Ordner assets/backup auf jeden Fall ignorieren.
# Kann Website Backup enthalten (große Datenmenge).
/assets/backup/*
/assets/db-dump/*
/assets/images/*
/assets/theme/*

# Composer-Zeug: wird absichtlich eingecheckt, damit die Site via
# "composer install" auf webserver nach vorgabe (composer.lock) gebaut wird.
# composer.json
# composer.lock

# config: verzeichnis mit konfigurationen für module etc.
# Falls hier YAML-Konfigurationen drin sind, mit einchecken.
# config

# drush: stuff für tool "drush", nicht zwingend erforderlich zu haben.
/drush/contrib/

# private: verzeichnis für private dateien
/private/*

# vendor: das verzeichnis mit allen durch composer geladenen abhängigen php-projekten
# wird nicht benötigt, wenn website mit "composer install" wiederhergestellt werden kann.
/vendor/

# spezielle unterverzeichnisse
# diese können ignoriert werden (wenn "composer install" möglich).
/web/core/
# libraries ggf. manuell per ftp
/web/libraries/
/web/modules/contrib/

# Ignore Drupal's file directory
/web/sites/*/files/

# settings dateien ignorieren, per ftp übertragen
# diese werden von drupal automatisch schreibgeschützt,
# was bei git-pull probleme bringt.
/web/sites/*/settings.php
/web/sites/*/settings/*
/web/sites/*/services.yml

# contrib profile und themes werden autom. installiert
/web/profiles/contrib/
/web/themes/contrib/

# gekauftes theme nicht einchecken, per ftp übertragen
/web/themes/custom/corporateplus

# Ignore SimpleTest multi-site environment.
/web/sites/simpletest
